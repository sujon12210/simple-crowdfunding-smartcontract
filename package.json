{
  "name": "simple-crowdfunding",
  "version": "1.0.0",
  "description": "Minimal crowdfunding smart contract",
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run deploy.js --network localhost"
  },
  "dependencies": {
    "ethers": "^6.10.0",
    "hardhat": "^2.22.0"
  }
}
